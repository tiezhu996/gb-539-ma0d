import {Component, inject} from '@angular/core'; import {NgFor} from '@angular/common'; import {AppStore} from '../stores/app.store';
@Component({standalone:true,imports:[NgFor],template:`<div class="page-head"><div><p class="eyebrow">05 / AUDIT</p><h1>审计中心</h1><p class="description">按实体、操作者和 request ID 回看每一次工艺决策。</p></div></div><section class="workspace-panel"><div class="panel-head"><div><h2>操作日志</h2><p>写操作保留前后快照。</p></div><span class="count">{{store.audits().length}} 条</span></div><div class="table-wrap"><table><tr><th>时间</th><th>实体</th><th>动作</th><th>操作者</th></tr><tr *ngFor="let item of store.audits()"><td>{{item.created_at}}</td><td><span class="entity">{{item.entity}}</span></td><td>{{item.action}}</td><td>{{item.actor_id}}</td></tr></table></div></section>`}) export class AuditPageComponent {readonly store=inject(AppStore)}
