# gb-539 木材窑干燥曲线自适应优化

请生成一个名为 `timber-kiln-drying-optimizer` 的 Go 全栈 Web 项目，面向木材加工企业的干燥工艺团队，用于维护干燥窑和木材批次、导入含水率读数、评估干燥阶段并生成可解释的曲线调整建议。系统仅做离线决策支持和仿真，不连接或控制窑炉、风机、蒸汽阀或喷淋设备；不得实现商城、电商、预约、订单、仓储库存、财务、客服工单、博客或大众社区。

## 1. 项目概述与复杂度下限

- 类型：B2B 前后端分离全栈 Web 应用。`TimberLot` 仅代表进入单次干燥过程的工艺批次，不实现库存、采购、销售或订单。
- 必须实现 4 个核心实体、5 个核心页面、至少 3 个共享组件、至少 2 个 hooks、至少 4 个后端中间件和 2 组前后端一致的共享枚举。
- 四实体必须贯穿 PostgreSQL、Go model/dto/repository/service/handler/router、Angular type/api/store/page。
- 本项目用于验证跨文件协同改动能力。严禁合并职责到单一文件；字段、状态、阶段、算法版本与权限变化必须触达至少 5 个文件。

## 2. 核心实体

1. `DryingKiln`（干燥窑）
   - 字段：`id`、`kiln_code`、`name`、`capacity_m3`、`max_temperature_c`、`min_humidity_pct`、`airflow_class`、`owner_team`、`kiln_state`、`commissioned_at`、`updated_at`。
   - 关系与动作：关联多个工艺批次和曲线；支持建档、更新安全边界、停用和查看历史摘要，不保存实时控制连接。
   - 必须贯穿 `drying_kilns` 表 -> 独立 model/dto/repository/service/handler/router -> `frontend/src/types/drying-kiln.ts`、api、store 和页面。
2. `TimberLot`（干燥工艺批次）
   - 字段：`id`、`lot_code`、`kiln_id`、`species`、`thickness_mm`、`volume_m3`、`initial_moisture_pct`、`target_moisture_pct`、`quality_grade`、`loaded_at`、`lot_state`、`created_by`。
   - 关系与动作：隶属干燥窑并拥有读数和计划；支持登记工艺属性、进入调湿/干燥/均衡阶段、完成或中止。
   - 必须贯穿 `timber_lots` 表及独立后端分层、前端 type/api/store/page 文件。
3. `MoistureReading`（含水率读数）
   - 字段：`id`、`timber_lot_id`、`sample_position`、`measured_at`、`moisture_pct`、`dry_bulb_c`、`wet_bulb_c`、`source_checksum`、`reading_quality`、`quality_note`、`imported_by`。
   - 关系与动作：隶属工艺批次并被计划快照引用；支持批量导入、范围/时间校验、作废与重导。
   - 必须贯穿 `moisture_readings` 表及独立后端分层、前端 type/api/store/page 文件。
4. `DryingSchedule`（干燥曲线计划）
   - 字段：`id`、`timber_lot_id`、`kiln_snapshot`、`algorithm_version`、`input_hash`、`stages_json`、`recommended_changes_json`、`predicted_finish_at`、`defect_risk_score`、`schedule_state`、`explanation`、`calculated_at`、`reviewed_by`。
   - 关系与动作：冻结窑炉边界、批次和读数；支持生成、版本对比、人工接受、退回修订和作废，不下发设备参数。
   - 必须贯穿 `drying_schedules` 表及独立后端分层、前端 type/api/store/page 文件。

## 3. 差异化算法与状态流

- 根据树种、厚度、初始/目标含水率与读数分布判断 `green/fiber_saturation/bound_water/target` 阶段，计算平均值、中心-表层梯度、单位时间降湿率与异常点。
- 使用版本化规则表限制每阶段的最高温度、最低相对湿度、最大降湿率与最大梯度；在安全约束内搜索小步调整建议，并解释每条建议对应的规则、输入和风险变化。不得输出超出窑炉安全边界的参数。
- `TimberLot`/计划状态流：`queued -> conditioning -> drying -> equalizing -> completed`，任一运行阶段可到 `aborted`；状态仅记录工艺仿真进度，不触发设备。非法迁移返回 409。
- `DryingSchedule` 计算为 `draft -> calculating -> proposed/failed -> reviewed -> accepted/voided`；同一输入哈希和算法版本幂等，历史版本不可覆盖。
- 算法必须有固定样例、边界测试和逐规则证据。README 标注建议不能替代窑干工艺师判断和现场安全联锁。

## 4. 核心页面与实体消费

1. `/kilns` 窑炉档案：消费 `DryingKiln + TimberLot`，展示边界、状态和当前工艺批次摘要。
2. `/lots` 工艺批次：消费 `TimberLot + DryingKiln + MoistureReading`，维护树种厚度与阶段状态。
3. `/readings` 含水率工作台：消费 `MoistureReading + TimberLot`，导入读数并展示梯度和质量问题。
4. `/schedules` 曲线优化：消费 `DryingSchedule + TimberLot + MoistureReading + DryingKiln`，展示阶段曲线、建议、风险和版本对比。
5. `/audit` 审计中心：消费四实体变更投影与审计记录，按 request ID、实体、操作者和时间筛选。

`MoistureStageBadge` 被批次、读数和计划页共用；`DryingCurveChart` 被读数和计划页共用；`RuleEvidenceDrawer` 被窑炉、计划和审计页共用。

## 5. 横切关注点

- JWT + RBAC：角色至少为 `admin`、`kiln_engineer`、`quality_analyst`、`reviewer`、`auditor`；联动角色表、JWT、Go auth/rbac、路由、Angular guard、按钮显隐和类型。计划创建者不能接受自己的建议。
- 审计与追踪：写操作记录前后快照、操作者和 request ID；算法记录输入 checksum、规则/算法版本、参数、耗时与摘要。
- 全局错误：读数或规则无效返回 422，状态/版本冲突返回 409，权限返回 403，前端统一处理。
- 限流与幂等：登录、读数导入和曲线计算接口分别限流；计算使用 `Idempotency-Key`。

## 6. 共享枚举、组件、hooks 与中间件

- `MoistureStage = green | fiber_saturation | bound_water | target`：后端 `backend/internal/constants/moisture_stage.go`，前端 `frontend/src/types/enums/moisture-stage.ts`。
- `ScheduleState = queued | conditioning | drying | equalizing | completed | aborted`：后端 `backend/internal/constants/schedule_state.go`，前端 `frontend/src/types/enums/schedule-state.ts`。
- README 列出两组枚举在 model、dto、算法、service 状态机、store、组件、页面和测试中的所有位置。
- 共享组件：`MoistureStageBadge`、`DryingCurveChart`、`RuleEvidenceDrawer`；Angular inject helpers：`useAuth`、`useScheduleSimulation`，位于 `frontend/src/hooks/`。
- 后端至少独立实现 `request_id.go`、`recovery.go`、`auth.go`、`rbac.go`、`audit.go`、`error_handler.go`。

## 7. 技术、规模与文件结构

- 英文短名：`timber-kiln-drying-optimizer`。
- 前端：Angular 17 + TypeScript + Vite + Angular Material + Signals store + ECharts；后端：Go 1.22 + Gin + GORM；正式 PostgreSQL 16，runtime smoke 支持 SQLite；JWT。
- API 统一使用 `/api/v1`，另提供真实 `/healthz`；使用 validator/v10、统一响应和错误码、`%w` 错误链、request ID、结构化日志、构造器注入并实现优雅停机。多表写入用事务，状态迁移用条件更新，唯一性/幂等不能只靠前端。禁止 TODO、placeholder、空函数、硬编码成功响应和 handler 直连数据库。
- Go 功能代码 2500-4200 行、24-42 个功能 `.go` 文件且少于 5000 行，不计 `_test.go`、生成代码和 vendor。阶段判断、约束搜索、状态机和权限须有测试。
- 只保留普通项目单测；不得创建名称含 record/regression/grading/scoring 的评分测试，不得加入答案、`BUG_REPRO.md` 或标注流水线内部文件。
- 根 `go.work` 纳入 `./backend`，支持根执行 `go work sync`、`go build ./backend/...`、`go test ./backend/...`。

```text
.
├── go.work  runtime_smoke.json
├── docker-compose.yml  .env  .env.example  .gitignore  README.md
├── output/
│   └── execution.md
├── backend/
│   ├── go.mod  Dockerfile  cmd/server/main.go
│   └── internal/
│       ├── config/ constants/ dto/ model/
│       ├── repository/ service/ handler/ router/
│       ├── middleware/ algorithm/ rules/ util/
├── frontend/
│   ├── package.json  Dockerfile  nginx.conf
│   └── src/
│       ├── api/ stores/ types/ components/common/
│       ├── hooks/ pages/ router/ utils/
└── database/init.sql
```

四实体必须在所有后端层和前端 api/store/type 中按实体分文件。严禁合并职责到单一文件，不得使用一个通用 CRUD handler/store 承载全部实体。

## 8. 运行 Manifest、部署与验证交付

- 固定端口：前端 `18539`、后端 `19539`、PostgreSQL 宿主机 `57539`；容器内 `80/8080/5432`。
- 根 `runtime_smoke.json` 的唯一用途是提供可解析的启动 manifest，内容必须严格等价于：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20539",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "JWT_SECRET": "runtime-smoke-only-change-me"
  },
  "ready_url": "http://127.0.0.1:20539/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

- 后端须按 manifest 自动创建 SQLite 表与演示数据；正式 Compose 仍用 PostgreSQL。manifest 禁止出现 passed、截图和执行结果字段。
- Compose 顶层 `name: timber-kiln-drying-optimizer` 且无 `version:`；编排 db/backend/frontend，使用命名卷和英文短名容器名前缀，数据库、后端和前端均有 healthcheck，并按健康条件依赖。
- `.env`、`.env.example` 均含 `COMPOSE_PROJECT_NAME=timber-kiln-drying-optimizer`、`FRONTEND_PORT=18539`、`BACKEND_PORT=19539`、`DB_PORT=57539`、`DB_DRIVER=postgres`、DB 变量和 `JWT_SECRET`；`.env` 不提交。前端只访问 `/api`，Nginx 原样代理 `/api/v1` 到 `backend:8080`，另提供 `/api/healthz` 并支持 SPA。
- README 首屏给 `docker compose up -d`，说明项目介绍、主要功能、角色账号、架构与目录、API 清单、技术栈表、环境变量与端口、本地开发、Docker 部署、枚举位置、算法与安全边界、runtime smoke、构建测试、常见问题、停止方式和 License；README 不得承载执行证据。
- 必须真实执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`、`npm --prefix frontend ci`、`npm --prefix frontend run build`、`docker compose config --quiet`、`docker compose up -d --build`、`docker compose ps`；前后端均须有 healthcheck且全部 healthy。至少 8 个真实 API 冒烟覆盖 health、登录、四实体、创建/导入、合法/非法迁移、优化运行/读取和 403。
- API 冒烟还必须包含详情读取、未认证 401 与越权 403；四个核心实体各至少经过一条真实后端链路，五个页面都消费真实 `/api/v1`，不得使用静态 mock。并在 `backend/` 内复跑 `go build ./...`、`go vet ./...`、`go test ./...`。
- 仅使用 Codex 内置 Browser 验证登录、窑炉、批次、读数、曲线五页，禁止外部 Chrome；实际完成创建或编辑、列表刷新和一次状态变化，并检查 console/network、文字溢出和遮挡。
- 所有构建、测试、runtime smoke、Compose、API、Browser、console/network、关闭证据只能写入 `output/execution.md`；截图直接放 `output/`，至少桌面和移动端各一张并由 execution.md 引用，禁止在其他文件复制证据或伪造通过。
- `output/execution.md` 必须包含项目编号、验证时间、实际端口、规模实测、根与 backend 构建/vet/test、前端构建、服务 healthy、API 表、Browser 操作步骤、截图相对路径、逐条需求覆盖、修复摘要、停服无残留和实现提交 hash。
- 验证后执行 `docker compose down -v --remove-orphans`，确认容器、网络和命名卷均已清理，并停止 runtime smoke 进程。
- 项目必须独立 `git init`，设置本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。真实实现与验证完成后提交 `git add -A && git commit -m "feat: implement and verify timber-kiln-drying-optimizer"`，把实现提交 hash 写入 `output/execution.md`，再执行 `git add output/execution.md && git commit -m "docs: record verified execution"`，形成两次提交。`.gitignore` 排除 `.env`、依赖、构建产物、日志和本地 SQLite 文件，但保留 execution.md 与截图；最终 `git status --short` 为空，不得共用 Git 仓库。
